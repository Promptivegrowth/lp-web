node_modules/
dist/
.vercel/
.DS_Store
Thumbs.db
*.log
.env
.env.*

# Fotografías originales (~148 MB). Se conservan fuera del repositorio;
# lo versionado es public/img/ ya optimizado. Ver README.
fotos/
# PDF originales del cliente; la versión web se genera en public/documentos/.
/documentos/

# Documentos de trabajo que envía el cliente (no forman parte del sitio).
*.docx

# Fotos que el cliente envía para revisar (originales pesados).
/fotos observadas/
